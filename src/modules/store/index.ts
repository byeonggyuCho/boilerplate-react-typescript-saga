import reducer from '@modules/store/reducer';
import saga from '@modules/store/saga';
import * as actions from '@modules/store/action';
import { PlanType, StoreType } from '@config/codes';

export default {
  reducer,
  actions,
  saga,
};
